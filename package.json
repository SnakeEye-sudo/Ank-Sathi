{
  "name": "ank-sathi",
  "private": true,
  "version": "1.0.0",
  "description": "Numerology utility app with life path, birth day, attitude, and name number calculations plus lightweight interpretations.",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=iife --target=es2020 --outfile=app.js"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
